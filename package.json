{
  "name": "openclaw-gigachat-stack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gateway:local": "openclaw start --config ./config/openclaw.local.json --port 3001",
    "mock:gateway": "node scripts/dev/mock-gateway.js"
  },
  "devDependencies": {
    "openclaw": "^2026.3.28"
  }
}
